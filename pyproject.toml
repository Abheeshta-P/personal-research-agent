[project]
name = "codespaces-blank"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Abheeshta P", email = "abheeshta.p.sclera@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "langchain>=1.3.15",
    "langchain-google-genai>=4.3.3",
    "langgraph>=1.2.11",
    "python-dotenv>=1.2.2",
    "requests>=2.34.2",
]

[project.scripts]
codespaces-blank = "codespaces_blank:main"

[build-system]
requires = ["uv_build>=0.12.4,<0.13.0"]
build-backend = "uv_build"
